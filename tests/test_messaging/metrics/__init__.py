"""RabbitMQ metrics test package.

This package contains tests for verifying metrics collection in the RabbitMQ connection manager:
- Connection metrics (attempts, successes, failures)
- Channel metrics (creation attempts, errors)
- Health check metrics (check attempts, consecutive failures)
"""
